if [[ $# == 0 ]]; then
    echo + ls -hlAF -rt >&2
    ls -hlAF -rt
elif [[ $# == 1 ]]; then
    echo + ls -hlAF -rt "$1" >&2
    ls -hlAF -rt "$1"
else
    echo + ls -AdhlF -rt ... >&2
    ls -dhlAF -rt "$@"
fi
