if [[ $# != 0 ]]; then
    set -xe
    git show --pretty= --name-only "$@"
else
    diffs=$(git diff --name-only)
    if [[ "$diffs" ]]; then
        set -xe
        git diff --name-only
    else
        set -xe
        git diff --name-only
        git show --pretty= --name-only
    fi
fi
