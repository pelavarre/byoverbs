if [[ $# == 0 ]]; then
    echo "+ git ls-files" "${ALTPWDS[@]}" >&2
    git ls-files "$ALTPWDS_0" "$ALTPWDS_1"
else
    echo "+ git ls-files" "$ALTPWDS_0" "$ALTPWDS_1" "|grep -i" "$@" >&2
    git ls-files "$ALTPWDS_0" "$ALTPWDS_1" |grep -i "$@"
fi
