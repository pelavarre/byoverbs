if [ -t 0 ] && [ -t 1 ]; then
    set -xe
    pbpaste |sed 's,^[ ][ ][ ][ ],,' |pbcopy
elif [ -t 0 ]; then
    pbpaste |sed 's,^[ ][ ][ ][ ],,'
elif [ -t 1 ]; then
    sed 's,^[ ][ ][ ][ ],,' |pbcopy
else
    sed 's,^[ ][ ][ ][ ],,'
fi
