if [ $# -eq 0 ]; then
    echo ': q && git checkout' >&2
    : q && git checkout
else
    echo ': q ... && git config --get-regexp "^alias[.]..." && git ...' >&2
    : q "$1" && git config --get-regexp "^alias[.]$1" && git "$@"
fi
