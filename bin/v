if [[ "$STY" ]]; then
    if "$(uname)" == "Darwin"; then
        unset TERM
    fi
fi

if [[ $# == 0 ]]; then
    set -xe
    pbpaste >v.cv
    vim v.cv
    <v.cv pbcopy
else
    set -xe
    vim "$@"
fi
